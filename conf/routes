# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Materia.index()
GET     /product                    controllers.Materia.index()
GET     /product/arrival            controllers.Materia.newArrival()
POST    /product/arrival            controllers.Materia.arrival()
#POST    /product/shipping           controllers.Materia.shipping()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
